spring:
  config:
    import: "optional:configserver:"

  cloud:
    config:
      enabled: false

  application:
    name: submission-service-test

  datasource:
    url: jdbc:h2:mem:submissiontestdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

eureka:
  client:
    enabled: false
