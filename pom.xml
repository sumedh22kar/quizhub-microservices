<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Inherit from Spring Boot -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.quizhub</groupId>
    <artifactId>quizhub</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <packaging>pom</packaging>

    <name>QuizHub Microservices</name>

    <description>
        Enterprise Spring Boot Microservices Project
    </description>

    <properties>

        <java.version>17</java.version>

        <spring-cloud.version>2025.0.3</spring-cloud.version>

    </properties>

    <modules>
        <module>backend/config-server</module>
        <module>backend/discovery-server</module>
        <module>backend/api-gateway</module>
        <module>backend/auth-service</module>
        <module>backend/quiz-service</module>
        <module>backend/question-service</module>
        <module>backend/submission-service</module>
        <module>backend/result-service</module>
        <module>backend/ai-agent-service</module>
    </modules>

    <dependencyManagement>

        <dependencies>

            <dependency>

                <groupId>org.springframework.cloud</groupId>

                <artifactId>spring-cloud-dependencies</artifactId>

                <version>${spring-cloud.version}</version>

                <type>pom</type>

                <scope>import</scope>

            </dependency>

        </dependencies>

    </dependencyManagement>

</project>